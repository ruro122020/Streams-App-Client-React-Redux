//created a history object we can maintain so we can use it in action creators

//history was installed as a dependency automatically when react-router-dom was installed
import { createBrowserHistory } from "history";

export default createBrowserHistory();
